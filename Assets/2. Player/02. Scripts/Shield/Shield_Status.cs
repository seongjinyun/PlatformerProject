using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shield_Status : Stat
{
    // Start is called before the first frame update
    void Start()
    {
        Attack_power = 1;
        Move_speed = 9.0f;
        Special_Attack = 5.0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
